services:
  # ============================================
  # 1. Backend Service (Node.js API)
  # ============================================
  - type: web
    name: trey-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_CALLBACK_URL
        sync: false
      - key: FRONTEND_URL
        fromService:
          type: web
          name: trey-frontend
          envVarKey: RENDER_EXTERNAL_URL

  # ============================================
  # 2. Frontend Service (React SPA)
  # ============================================
  - type: web
    name: trey-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: CI
        value: "false" # This prevents the build from failing on minor warnings
      - key: NODE_VERSION
        value: "20"
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: trey-backend
          envVarKey: RENDER_EXTERNAL_URL